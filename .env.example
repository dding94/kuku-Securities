# Kuku Order System 환경 변수 템플릿
# 로컬 개발: 이 파일을 복사하여 .env로 저장하고 사용하세요.
# 운영 환경: K8s Secret, AWS Parameter Store 등으로 주입하세요.

# Database (로컬 개발용 예시)
DB_URL=jdbc:mysql://localhost:3306/kuku_order?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=root

# JPA Debug (개발 환경에서는 true, 운영에서는 false)
SHOW_SQL=true
FORMAT_SQL=true
